type RelatedContentItem {
    title: String
    url: String!
}

type RelatedContentItemsResult {
    message: String
    relatedItems: [RelatedContentItem]
}

type Query {
    getSyndicatedRelatedContentFor(syndicated_article_api: String!): RelatedContentItemsResult
}